{
  "buildCommand": "cd Editable-Document && npm install && npm run build",
  "outputDirectory": "Editable-Document/dist",
  "functions": {
    "api/gerar-pdf.js": {
      "runtime": "nodejs18.x",
      "maxDuration": 30
    }
  }
}
